{
    "if": {
        "allOf": [
            {
                "field": "type",
                "equals": "Microsoft.Network/virtualNetworks"
            },
            {
                "anyOf": [
                    {
                        "value": "[equals(length(intersection(parameters('dnsSettings'), field('Microsoft.Network/virtualNetworks/dhcpOptions.dnsServers'))), length(parameters('dnsSettings')))]",
                        "equals": false
                    },
                    {
                        "value": "[equals(length(field('Microsoft.Network/virtualNetworks/dhcpOptions.dnsServers')),length(parameters('dnsSettings')))]",
                        "equals": false
                    }
                ]
            }
        ]
    },
    "then": {
        "effect": "deny"
    }
}